# 기후변화로부터 지구를 지켜라! — 진행 지원 웹앱 명세서

> 이 문서를 프로젝트 루트에 `SPEC.md`로 두고 Claude Code에게 "SPEC.md를 읽고 구현해줘"라고 지시하세요.
> 숫자와 규칙은 임의로 바꾸지 말 것. 명시되지 않은 부분은 반드시 사용자에게 질문할 것.

---

## 0. 한 줄 요약

교실에서 6개 조가 8턴 동안 진행하는 기후 협상 보드게임의 **진행·계산·타이머·집계**를 자동화하는 실시간 웹앱.
조별 태블릿에서 개발 방식을 비밀 제출하고, 교사 노트북에서 진행을 통제하며, 프로젝터/전자칠판에 현황을 띄운다.

---

## 1. 화면 구성 (3종)

| 화면 | 기기 | 경로 | 역할 |
|---|---|---|---|
| 교사 콘솔 | 노트북 | `/host/[roomCode]` | 페이즈 진행, 타이머 제어, 퀴즈 채점, 능력 승인, 수동 보정 |
| 관전 화면 | 프로젝터/전자칠판 | `/board/[roomCode]` | 읽기 전용. 기온·지구 상태·GP 순위·타이머 대형 표시 |
| 팀 화면 | 조별 태블릿 6대 | `/play/[roomCode]` | 국가 선택 후 개발 방식 제출, 자국 GP·능력 확인 |

- 방 생성 시 6자리 방 코드 발급. 팀은 코드 입력 → 국가 선택(선점 방식, 중복 불가).
- **새로고침·연결 끊김 복구 필수.** 서버 상태가 진실의 원천이고, 클라이언트는 항상 서버 상태를 다시 그린다.

---

## 2. 초기값

```json
{
  "temperature": 15.0,
  "countries": [
    { "id": "kor", "name": "대한민국", "gp": 15, "ability": "녹색성장" },
    { "id": "usa", "name": "미국",     "gp": 18, "ability": "CCS기술" },
    { "id": "swe", "name": "스웨덴",   "gp": 20, "ability": "인간환경선언" },
    { "id": "dnk", "name": "덴마크",   "gp": 20, "ability": "코펜하겐기후협약" },
    { "id": "jpn", "name": "일본",     "gp": 18, "ability": "교토의정서" },
    { "id": "tuv", "name": "투발루",   "gp": 8,  "ability": "가라앉는섬" }
  ],
  "totalTurns": 8,
  "totalSnacks": 32
}
```

---

## 3. 턴 진행 순서 (상태 머신)

각 턴은 아래 페이즈를 순서대로 지난다. 교사가 "다음" 버튼으로 넘기며, 타이머는 자동 시작·수동 일시정지 가능.

| 턴 | 페이즈 순서 |
|---|---|
| 1턴 | 개발선택(30초) → 기후변화 퀴즈(1분) |
| 2턴 | 국가별 협의(30초) → 대표회의(3분) → 개발선택(30초) → 기후변화 퀴즈(1분) |
| 3턴 | 국가별 협의(30초) → 대표회의(3분) → 개발선택(30초) → 기후변화 퀴즈(1분) |
| 4턴 | 국가별 협의(30초) → 대표회의(3분) → 개발선택(30초) → **UN 환경보전회의(1분)** |
| 5턴 | 국가별 협의(30초) → 대표회의(3분) → 개발선택(30초) → 기후변화 퀴즈(1분) |
| 6턴 | 국가별 협의(30초) → 대표회의(3분) → 개발선택(30초) → 기후변화 퀴즈(1분) |
| 7턴 | 국가별 협의(30초) → 개발선택(30초) |
| 8턴 | 국가별 협의(30초) → 개발선택(30초) → **자원 배분(1분)** |

- 퀴즈는 총 5회(1·2·3·5·6턴), 문항 5개와 정확히 일치한다.
- **7·8턴에는 대표회의와 특수능력 사용이 없다.** UI에서 아예 잠글 것.

---

## 4. 개발 선택과 기온 계산

### 4.1 선택지

| 선택 | 획득 GP |
|---|---|
| 경제 우선 개발 | 10 |
| 균형 개발 | 8 |
| 환경 우선 개발 | 5 |

- 6개 조가 태블릿에서 **비밀 제출**. 제출 현황은 "3/6 제출 완료"처럼 인원수만 공개하고 내용은 감춘다.
- 전원 제출 또는 시간 종료 시 교사가 "공개" 버튼을 누르면 6개국 선택이 동시에 뒤집히며 공개된다.
- 미제출 조는 교사가 대신 입력할 수 있어야 한다.

### 4.2 기온 상승 (핵심 규칙)

이번 턴 6개국이 **획득한 개발 GP의 합계**(30~60)를 구간표에 대입한다.

| GP 합계 | 35 이하 | 36–40 | 41–45 | 46–50 | 51–55 | 56–60 |
|---|---|---|---|---|---|---|
| 기온 변화 | **−0.2** | +0.1 | +0.3 | +0.6 | +0.9 | +1.2 |

> 주의: 여기서 쓰는 값은 **각국 누적 보유 GP가 아니라 이번 턴에 새로 얻은 GP의 합**이다.
> 획득 GP는 각국 누적 GP에 더해진다.

### 4.3 퀴즈

- 교사가 O / X 판정 버튼을 누른다. 학급 전체가 맞히면 기온 유지, 못 맞히면 **기온 +0.1**.
- 문항은 `quiz.json`으로 분리해 교사가 쉽게 수정·추가할 수 있게 한다.

### 4.4 기온 표시

기온은 소수점 첫째 자리까지. 부동소수점 누적 오차를 피하려 **내부적으로 0.1도 단위 정수(150 = 15.0도)로 저장**하고 표시할 때만 나눈다.

---

## 5. 지구 상태 / 획득 자원

| 기온 | 지구의 상태 | 획득 자원 |
|---|---|---|
| 15.0 ~ 15.9 | 평온한 지구 | 초콜릿 |
| 16.0 ~ 16.9 | 변화하는 지구 | 라면 과자 |
| 17.0 ~ 17.9 | 다가오는 위험 | 포테이토 과자 |
| 18.0 ~ 18.9 | 아파하는 지구 | 비스킷 1개 |
| 19.0 ~ 19.9 | 위험에 빠진 인류 | 초코볼 한 알 |
| 20.0 이상 | **지구의 멸망** | 없음 (게임 종료, 아무도 획득 불가) |

- 각 구간의 환경 변화 설명문(PDF 2쪽 표)을 데이터로 넣어, 기온이 구간을 넘을 때마다 관전 화면에 경고 연출과 함께 띄운다. 이게 이 수업의 교육 효과 핵심이다.
- 20도 도달 시 즉시 종료 화면으로 전환.

---

## 6. 국가별 특수 능력

공통 규칙: **사용 시 해당국 GP −5**, 국가당 **1회만**, **7·8턴 사용 불가**, 한 턴에 여러 국가 동시 사용 가능, 공개 선언, 번복 불가.

| 국가 | 능력 | 효과 | 사용 조건 |
|---|---|---|---|
| 대한민국 | 녹색성장 | 지명한 두 나라가 가위바위보 → 진 쪽 GP −5, 기온 −0.3 | 없음 |
| 미국 | CCS 기술 | 퀴즈를 맞히면 기온 −0.5 (4턴은 교사와 가위바위보 승리 시 −0.5) | 없음 |
| 스웨덴 | 인간 환경 선언 | 해당 턴 전 국가가 '환경 우선 개발' 선택 시 기온 추가 −0.4 | 없음 |
| 일본 | 교토의정서 | 직전 턴 '경제 우선 개발'을 한 국가는 이번 턴 '환경 우선 개발' 강제 | 기온 17도 이상 |
| 투발루 | 가라앉는 섬 | 어떤 국가가 투발루에 GP 10을 주면 기온 −0.4 (전원 거부 시 무산) | 기온 18도 이상 |
| 덴마크 | 코펜하겐 기후협약 | GP 상위 3개국은 이번 턴부터 2턴간 '환경 우선 개발' 강제 (공동 3위 모두 포함) | 기온 17도 이상 |

### 구현 방침 — 중요

**능력을 완전 자동화하지 말 것.** 가위바위보·협상 결과처럼 앱 밖에서 벌어지는 일이 섞여 있다.

- 팀 화면: "능력 사용 요청" 버튼 (조건 미충족 시 비활성 + 사유 표시)
- 교사 콘솔: 요청 승인 → 결과 입력(가위바위보 승자, GP 수령국 등) → 효과 적용
- 강제 선택(교토·코펜하겐)은 대상 팀의 태블릿에서 다른 선택지를 **잠그고** 사유를 표시한다. 덴마크 능력은 2턴 지속이므로 잔여 턴수를 상태로 관리할 것.

---

## 7. UN 환경보전회의 (4턴 종료 후 1회)

각국의 **누적 보유 GP**로 판정한다.

| 보유 GP | 평가 | 보상/제재 |
|---|---|---|
| 45 이하 | 지속가능한 개발 상태 | 선택한 국가에서 GP 5를 빼앗아 온다 |
| 46 ~ 54 | 녹색개발 상태 | 없음 |
| 55 이상 | 환경파괴 상태 | 다른 모든 국가에게 GP를 1씩 나누어 준다 |

- "지속가능" 국가는 대상국을 지정해야 하므로, 교사 콘솔에서 국가별로 대상 선택 UI를 띄우고 확정 버튼으로 일괄 적용한다.
- 적용 전후 GP 변화를 관전 화면에 표로 보여줄 것.

---

## 8. 최종 자원 배분

```
각국 배분 과자 수 = round( 최종 GP / 전체 GP 합계 × 32 )
```

- 반올림 오차로 합계가 32와 어긋나면, 소수점 나머지가 큰 순서로 1개씩 조정해 정확히 32개를 맞춘다.
- 기온이 20도 이상이면 전원 0개.
- 최종 화면: 국가별 최종 GP, 비율, 과자 수, 지구 최종 기온과 상태, 8턴간 기온 변화 그래프.

---

## 9. 교사용 안전장치 (필수)

수업 중 사고 복구가 안 되면 앱이 없느니만 못하다. 아래는 필수 기능이다.

1. **수동 보정 패널** — 임의 국가의 GP, 현재 기온을 직접 수정
2. **되돌리기** — 직전 1개 액션 취소
3. **상태 저장/복원** — 방 상태를 JSON으로 내보내고 불러오기 (노트북이 꺼져도 복구)
4. **강제 페이즈 이동** — 타이머 무시하고 임의 페이즈로 점프
5. **팀 재접속** — 태블릿이 끊겨도 같은 국가로 재입장 가능

---

## 10. 기술 스택 제안

- **Next.js (App Router) + TypeScript + Tailwind CSS**
- **Supabase** — Realtime Broadcast로 상태 동기화, 방 상태는 단일 `rooms` 레코드의 JSONB 컬럼에 통째로 저장 (룰이 복잡하지 않아 정규화보다 단순함이 유리)
- **Vercel** 배포
- 상태 변경은 **전부 서버(Route Handler 또는 Supabase RPC)에서 처리**한다. 클라이언트가 GP를 계산하면 태블릿에서 조작 가능해진다.
- 로그인 없음. 방 코드만으로 입장.

---

## 11. UI 요구사항

- **관전 화면은 교실 뒤에서 읽힌다는 전제.** 기온 숫자는 화면 높이의 1/4 이상, 타이머도 대형.
- 기온 구간이 바뀌면 배경색이 단계적으로 변한다 (평온=청록 → 멸망=적색).
- 태블릿 화면은 터치 타깃 최소 60px, 중학생이 설명 없이 쓸 수 있을 만큼 단순하게.
- 모든 문구는 한국어.

---

## 12. 구현 순서 (Claude Code에 이 순서로 시키세요)

1. 게임 규칙을 순수 함수 모듈(`lib/rules.ts`)로 구현 + 단위 테스트 (기온 구간, 배분 계산, 능력 조건)
2. 로컬 상태만 쓰는 **단일 화면 교사용 프로토타입** — 여기서 룰이 맞는지 직접 8턴 돌려본다
3. Supabase 연동 + 실시간 동기화
4. 팀 화면, 관전 화면 분리
5. 안전장치(9번) 구현
6. 디자인 다듬기

> 2번 단계에서 반드시 한 판 완주해 보세요. 룰 오류는 여기서 다 잡힙니다.
